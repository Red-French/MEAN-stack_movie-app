module.exports = {
  // key  // value
  'movie': require('./controllers/MovieController')  // At the 'movie' route, load and execute the controller
};                                                   // MovieController is assigned to the path 'movie'